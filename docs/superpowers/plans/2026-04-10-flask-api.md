# Flask API 接口服务 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 Spider_XHS 项目以 Flask API 形式暴露 4 个核心接口：获取用户所有笔记、获取笔记信息、搜索笔记、获取笔记评论。

**Architecture:** 新增一个 `app.py` 文件作为 Flask 应用入口，直接调用 `apis/xhs_pc_apis.py` 中的 `XHS_Apis` 类方法获取数据，使用 `xhs_utils/data_util.py` 中的处理函数加工返回值。不改动任何现有文件。

**Tech Stack:** Flask, 现有 requests + PyExecJS 签名体系

---

### Task 1: 创建 Flask 应用骨架与统一响应格式

**Files:**
- Create: `app.py`

- [ ] **Step 1: 创建 `app.py` 基础结构**

```python
# encoding: utf-8
from flask import Flask, request, jsonify
from apis.xhs_pc_apis import XHS_Apis
from xhs_utils.data_util import handle_note_info, handle_comment_info

app = Flask(__name__)
xhs_apis = XHS_Apis()


def success_response(data):
    return jsonify({"code": 0, "msg": "success", "data": data})


def error_response(msg):
    return jsonify({"code": -1, "msg": str(msg), "data": None})
```

- [ ] **Step 2: 添加启动入口**

在 `app.py` 底部添加：

```python
if __name__ == '__main__':
    app.run(host='0.0.0.0', port=5000, debug=True)
```

- [ ] **Step 3: 验证应用可启动**

Run: `cd D:/develop/project/python/Spider_XHS && python -c "from app import app; print('OK')"`
Expected: `OK`

- [ ] **Step 4: Commit**

```bash
git add app.py
git commit -m "feat: add Flask app skeleton with response helpers"
```

---

### Task 2: 实现获取笔记信息接口

**Files:**
- Modify: `app.py`

**逻辑说明：** 调用 `xhs_apis.get_note_info(note_url, cookie)` 获取原始数据，然后参考 `main.py:22-26` 的处理方式：取 `res_json['data']['items'][0]`，设置 `url` 字段，再传入 `handle_note_info()` 加工。

- [ ] **Step 1: 添加获取笔记信息路由**

在 `app.py` 的 `xhs_apis = XHS_Apis()` 之后添加：

```python
@app.route('/api/note/info', methods=['POST'])
def api_get_note_info():
    data = request.get_json()
    if not data or 'cookie' not in data or 'note_url' not in data:
        return error_response('missing required fields: cookie, note_url')
    cookie = data['cookie']
    note_url = data['note_url']
    success, msg, res_json = xhs_apis.get_note_info(note_url, cookie)
    if not success:
        return error_response(msg)
    try:
        note_data = res_json['data']['items'][0]
        note_data['url'] = note_url
        note_info = handle_note_info(note_data)
        return success_response(note_info)
    except Exception as e:
        return error_response(str(e))
```

- [ ] **Step 2: 验证路由注册**

Run: `cd D:/develop/project/python/Spider_XHS && python -c "from app import app; rules = [r.rule for r in app.url_map.iter_rules()]; print('/api/note/info' in rules)"`
Expected: `True`

- [ ] **Step 3: Commit**

```bash
git add app.py
git commit -m "feat: add GET note info API endpoint"
```

---

### Task 3: 实现获取用户所有笔记接口

**Files:**
- Modify: `app.py`

**逻辑说明：** 调用 `xhs_apis.get_user_all_notes(user_url, cookie)` 获取笔记列表（返回的是简要笔记对象列表，包含 `note_id` 和 `xsec_token`）。参考 `main.py:66-74`，对每条笔记构造完整 URL，再逐条调用 `get_note_info` + `handle_note_info` 获取详情。但为了接口响应速度，这里提供两种模式：默认返回简要列表（仅 note_id、xsec_token），可选 `detail=true` 获取完整详情。

- [ ] **Step 1: 添加获取用户所有笔记路由**

在 `app.py` 中添加：

```python
@app.route('/api/user/notes', methods=['POST'])
def api_get_user_notes():
    data = request.get_json()
    if not data or 'cookie' not in data or 'user_url' not in data:
        return error_response('missing required fields: cookie, user_url')
    cookie = data['cookie']
    user_url = data['user_url']
    detail = data.get('detail', False)
    success, msg, note_list = xhs_apis.get_user_all_notes(user_url, cookie)
    if not success:
        return error_response(msg)
    if not detail:
        return success_response(note_list)
    # 获取每条笔记的详细信息
    detailed_notes = []
    for note in note_list:
        note_url = f"https://www.xiaohongshu.com/explore/{note['note_id']}?xsec_token={note['xsec_token']}"
        s, m, res_json = xhs_apis.get_note_info(note_url, cookie)
        if s:
            try:
                note_data = res_json['data']['items'][0]
                note_data['url'] = note_url
                detailed_notes.append(handle_note_info(note_data))
            except Exception:
                pass
    return success_response(detailed_notes)
```

- [ ] **Step 2: 验证路由注册**

Run: `cd D:/develop/project/python/Spider_XHS && python -c "from app import app; rules = [r.rule for r in app.url_map.iter_rules()]; print('/api/user/notes' in rules)"`
Expected: `True`

- [ ] **Step 3: Commit**

```bash
git add app.py
git commit -m "feat: add GET user all notes API endpoint"
```

---

### Task 4: 实现搜索笔记接口

**Files:**
- Modify: `app.py`

**逻辑说明：** 调用 `xhs_apis.search_some_note()` 搜索笔记。返回的结果中需要过滤 `model_type == "note"` 的条目（参考 `main.py:99`），然后逐条构造 URL 并调用 `get_note_info` + `handle_note_info` 获取详情。同样提供 `detail` 参数控制是否获取详情。

- [ ] **Step 1: 添加搜索笔记路由**

在 `app.py` 中添加：

```python
@app.route('/api/note/search', methods=['POST'])
def api_search_note():
    data = request.get_json()
    if not data or 'cookie' not in data or 'query' not in data or 'require_num' not in data:
        return error_response('missing required fields: cookie, query, require_num')
    cookie = data['cookie']
    query = data['query']
    require_num = data['require_num']
    sort = data.get('sort', 0)
    note_type = data.get('note_type', 0)
    detail = data.get('detail', False)
    success, msg, notes = xhs_apis.search_some_note(query, require_num, cookie, sort, note_type)
    if not success:
        return error_response(msg)
    notes = [n for n in notes if n.get('model_type') == 'note']
    if not detail:
        return success_response(notes)
    # 获取每条笔记的详细信息
    detailed_notes = []
    for note in notes:
        note_url = f"https://www.xiaohongshu.com/explore/{note['id']}?xsec_token={note['xsec_token']}"
        s, m, res_json = xhs_apis.get_note_info(note_url, cookie)
        if s:
            try:
                note_data = res_json['data']['items'][0]
                note_data['url'] = note_url
                detailed_notes.append(handle_note_info(note_data))
            except Exception:
                pass
    return success_response(detailed_notes)
```

- [ ] **Step 2: 验证路由注册**

Run: `cd D:/develop/project/python/Spider_XHS && python -c "from app import app; rules = [r.rule for r in app.url_map.iter_rules()]; print('/api/note/search' in rules)"`
Expected: `True`

- [ ] **Step 3: Commit**

```bash
git add app.py
git commit -m "feat: add search notes API endpoint"
```

---

### Task 5: 实现获取笔记评论接口

**Files:**
- Modify: `app.py`

**逻辑说明：** 调用 `xhs_apis.get_note_all_comment(note_url, cookie)` 获取全部评论（含子评论）。返回的是原始评论对象列表，每条评论用 `handle_comment_info()` 加工。子评论在 `sub_comments` 字段中，也需要逐条加工。

- [ ] **Step 1: 添加获取笔记评论路由**

在 `app.py` 中添加（在 `if __name__` 之前）：

```python
@app.route('/api/note/comments', methods=['POST'])
def api_get_note_comments():
    data = request.get_json()
    if not data or 'cookie' not in data or 'note_url' not in data:
        return error_response('missing required fields: cookie, note_url')
    cookie = data['cookie']
    note_url = data['note_url']
    success, msg, comments = xhs_apis.get_note_all_comment(note_url, cookie)
    if not success:
        return error_response(msg)
    result = []
    for comment in comments:
        comment['note_url'] = note_url
        comment_info = handle_comment_info(comment)
        # 处理子评论
        sub_comments = []
        for sub in comment.get('sub_comments', []):
            sub['note_url'] = note_url
            sub_comments.append(handle_comment_info(sub))
        comment_info['sub_comments'] = sub_comments
        result.append(comment_info)
    return success_response(result)
```

- [ ] **Step 2: 验证路由注册**

Run: `cd D:/develop/project/python/Spider_XHS && python -c "from app import app; rules = [r.rule for r in app.url_map.iter_rules()]; print('/api/note/comments' in rules)"`
Expected: `True`

- [ ] **Step 3: Commit**

```bash
git add app.py
git commit -m "feat: add GET note comments API endpoint"
```

---

### Task 6: 添加 flask 依赖并最终验证

**Files:**
- Modify: `requirements.txt`

- [ ] **Step 1: 在 `requirements.txt` 末尾添加 flask**

在文件末尾添加一行：

```
flask
```

- [ ] **Step 2: 验证完整的 app.py 可以导入**

Run: `cd D:/develop/project/python/Spider_XHS && python -c "from app import app; rules = [r.rule for r in app.url_map.iter_rules()]; expected = ['/api/note/info', '/api/user/notes', '/api/note/search', '/api/note/comments']; print(all(e in rules for e in expected))"`
Expected: `True`

- [ ] **Step 3: Commit**

```bash
git add requirements.txt app.py
git commit -m "feat: add flask dependency, complete API service setup"
```
